1. For the InterestCalculator with if/else:
If we needed to add a 4th account type, we would have to open the InterestCalculator.java file and edit the calculateInterest method directly. we'd have to add a new "else if (accountType.equals("Salary"))" block right in the middle of it.

5. Diff mentally: for the Salary Account requirement:
For the new Salary account, the ONLY things to do were create brand new files (SalaryAccount.java and SalaryInterestPolicy.java) and then update Main.java to use them. 
we can confirm that zero existing policy classes (like SavingsInterestPolicy) changed. Adding new features without touching old code works perfectly.
